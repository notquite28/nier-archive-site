name = "archive-site"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".svelte-kit/cloudflare"

# Required when Cloudflare manages vars via wrangler.toml (Git deploy). Replace with your Worker URL.
[vars]
VITE_WS_URL = "wss://cursor-server.arnav-panigrahi.workers.dev/ws"

[[kv_namespaces]]
binding = "ANALYTICS"
id = "5a8b88b6d6ba4f02abd130247b21ffb1"
